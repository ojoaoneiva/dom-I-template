// Implemente uma lógica que, toda vez que o usuário clicar em um botão, será impresso no console a quantidade de vezes que o botão foi clicado até o momento atual.

let contador = 0

function contar () {
    console.log(contador+=1)
}